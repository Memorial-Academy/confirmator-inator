{
  "name": "confirmator-inator",
  "version": "1.0.0",
  "description": "Confirmation emails",
  "main": "index.js",
  "scripts": {
    "prepare": "tsc && rm -rf data/output && mkdir data/output",
    "compile": "node dist/student.js",
    "build": "npm run prepare && npm run compile"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "csv-parse": "^5.4.0",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "@types/pug": "^2.0.6",
    "typescript": "^5.0.4"
  }
}
